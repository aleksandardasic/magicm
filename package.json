{
  "name": "magicmirror",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node dist/app.js",
    "compile": "rm -rf dist & tsc",
    "copy-static": "cp -r src/static/ dist/",
    "copy-db": "cp -r src/db/ dist/",
    "build": "npm run compile && npm run copy-static && npm run copy-db"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "nosql": "^5.0.3",
    "socket.io": "^2.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.0.37",    
    "@types/node": "^8.0.26",
    "@types/socket.io": "^1.4.30",
    "typescript": "^2.5.2"
  }
}